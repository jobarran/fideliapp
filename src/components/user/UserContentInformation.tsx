import { UserProfileData } from '@/interfaces'
import React from 'react'

interface Props {
    user: UserProfileData
}

export const UserContentInformation = ({user}:Props) => {
  return (
    <div>UserContentInformation</div>
  )
}
