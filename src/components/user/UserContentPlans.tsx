import { UserProfileData } from '@/interfaces'
import React from 'react'

interface Props {
    user: UserProfileData
}

export const UserContentPlans = ({user}:Props) => {
  return (
    <div>Plans</div>
  )
}
